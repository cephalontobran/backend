describe("importer", () => {
  it.todo("should assemble a list of warframes")
  it.todo("must fail fatally if the Firebase credential is missing")
  it.todo("must fail if warframes cannot be saved")
})
